import React from 'react';


const Saveprod = () => {
  const handleOnSubmit = (prod) => {
    console.log(prod);
  };

  return (
    <React.Fragment>
      {/* <Admin handleOnSubmit={handleOnSubmit} /> */}
    </React.Fragment>
  );
};

export default Saveprod;